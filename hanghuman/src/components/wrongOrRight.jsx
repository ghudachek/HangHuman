import React from "react";
import Alphabet from "./alphabet";
import Level from "./levels";

const wrongOrRight = (props) => {
  props.selectedWord.split("");
  console.log(props.selectedWord.split(""));

  return <div></div>;
};

export default wrongOrRight;
